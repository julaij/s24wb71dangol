extends layout

block content
  h1= title
  p Welcome to Julai Dangol's Web Application using #{title} and Pug
  button(type="button", class="btn btn-secondary", onclick="double()") I am a button
  div#double Up the amount
  a(href="/artists") Go to Artists Page

  script.
    var value = 0;
    var power = 1;
    
    function double() {
      value += 1;
      power *= 2;
      document.getElementById('double').innerText = `Two to the power ${value} is ${power}`;
    }